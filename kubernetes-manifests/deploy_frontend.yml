apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: minimal-boutique
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      # nodeName: petshopboys
      containers:
        - name: frontend
          image: ghcr.io/computernetworks-ufrgs/minimal-boutique-frontend
          ports:
            - containerPort: 5173
          command: ["npm", "run", "dev", "--", "--host"]
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: minimal-boutique
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173
  type: ClusterIP
